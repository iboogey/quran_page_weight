name: quran_page_weight
description: >-
  Compute the Madani mushaf (604-page, Hafs) page weight of any Quran ayah
  range. Range → pages, reverse lookup, and sura page info. Offline, zero
  dependencies, pure Dart.
version: 0.1.0
environment:
  sdk: ^3.0.0
dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
